#include <stdio.h>
#include <math.h>
int main(void){
    int e, f, c, t, res;
    scanf("%d", &e, &f, &c);
    res = (e + f)/c;
    
    printf("%d\n",(e + f) / c);
    getch();
    return 0;
}
