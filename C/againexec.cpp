#include <stdio.h>
#include <errno.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
int main(int argc, char** argv){
    int i;
  char buffer[100];
  gets(buffer];
  char * ex[25];
  for(i = 0; i < 25; i++)
    ex[i] = (char*) malloc(20);
 while( sscanf(buffer, "%s", 
}
